diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 00cc97f..23f3f20 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -17,10 +17,21 @@ jobs:
         with:
           python-version: "3.12"
 
+      - name: Install system dependencies
+        run: |
+          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
+          wget -qO /usr/local/bin/hadolint https://github.com/hadolint/hadolint/releases/download/v2.12.0/hadolint-Linux-x86_64
+          chmod +x /usr/local/bin/hadolint
+
       - name: Install validation dependency
         run: |
           python -m pip install --upgrade pip
           pip install -r requirements.txt
+          npm ci
+          pip install pre-commit
+
+      - name: Run pre-commit hooks
+        run: pre-commit run --all-files
 
       - name: make validate
         run: make validate
@@ -36,3 +47,9 @@ jobs:
         with:
           name: dependency-manifest
           path: dependencies.txt
+
+      - name: Upload build artifacts
+        uses: actions/upload-artifact@4c1d4e2f6b7c9d0e1f2a3b4c5d6e7f8a9b0c1d2e # v4
+        with:
+          name: dist
+          path: dist/
